# Copyright 2026 fitctl contributors
# SPDX-License-Identifier: Apache-2.0

[package]
name = "fitctl-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Core library for surveying hosts, deriving contracts, and validating workload fit"
readme = "README.md"
repository = "https://github.com/tznurmin/fitctl"
homepage = "https://github.com/tznurmin/fitctl"
keywords = ["host", "survey", "policy", "validation"]
categories = ["development-tools"]

[lib]
path = "src/lib.rs"

[dependencies]
base64 = "0.22"
libc = "0.2"
minicbor = { version = "2.3", features = ["std", "half"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"

[dev-dependencies]
cbor4ii = { version = "1.2.2", features = ["serde1", "use_std", "half-f16"] }
